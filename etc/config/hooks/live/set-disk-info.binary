#!/bin/sh

echo "P: Begin executing set-disk-info binary hook in ${PWD}"

cp -rf ../config/includes.binary/.disk .

echo "P: Begin executing rpi-firmware binary hook in ${PWD}"

cp -rf ../chroot/usr/lib/firmware/* .
cp -rf ../chroot/usr/lib/linux-firmware-raspi/* .