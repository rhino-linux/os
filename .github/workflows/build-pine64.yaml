name: Build PINE64 Images

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.platform }} ${{ matrix.environment }} rootfs
    strategy:
      fail-fast: false
      matrix:
        platform:
          - pinephone
          - pinetab
        environment:
          - unicorn
          - lomiri
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4

      - name: Build rootfs
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list
          sudo rm -rf /var/lib/apt/lists/*
          sudo ./build.sh \
            "${{ matrix.platform }}" \
            "${{ matrix.environment }}" \
            "build_${{ matrix.platform }}_${{ matrix.environment }}"

      - name: Upload rootfs
        uses: actions/upload-artifact@v7
        with:
          name: rootfs-${{ matrix.platform }}-${{ matrix.environment }}
          path: build_${{ matrix.platform }}_${{ matrix.environment }}/binary
          if-no-files-found: error
          retention-days: 1
          compression-level: 0

  deploy:
    name: Deploy ${{ matrix.platform }} ${{ matrix.environment }} image
    needs: build
    if: ${{ !cancelled() }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - pinephone
          - pinephonepro
          - pinetab
          - pinetab2
        environment:
          - unicorn
          - lomiri
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download rootfs
        uses: actions/download-artifact@v8
        with:
          name: rootfs-${{ matrix.platform }}-${{ matrix.environment }}
          path: build_${{ matrix.platform }}_${{ matrix.environment }}/binary

      - name: Deploy image
        run: |
          sudo ./deploy.sh \
            "${{ matrix.platform }}" \
            "${{ matrix.environment }}" \
            "build_${{ matrix.platform }}"

      - name: Compress image
        run: |
          xz -T0 -v "build_${{ matrix.platform }}/builds/Rhino"*.img
          echo "IMGNAME=$(cd "build_${{ matrix.platform }}/builds"; ls *.img.xz)" >> $GITHUB_ENV

      - name: Upload image
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.IMGNAME }}
          path: build_${{ matrix.platform }}/builds/${{ env.IMGNAME }}
          if-no-files-found: error
          compression-level: 0
